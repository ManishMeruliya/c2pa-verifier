import { defineConfig } from '@tailwindcss/vite'

export default defineConfig({
  theme: {
    extend: {
      colors: {
        primary: "#2563eb",
        accent: "#38bdf8",
        dark: "#0f172a",
      },
    },
  },
})
